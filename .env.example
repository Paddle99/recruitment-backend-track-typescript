# --- Main database URL ---
# Used by Prisma to connect to your main PostgreSQL database
# Format: postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>
DATABASE_URL="postgresql://user:password@localhost:5432/my_database"

# --- PostgreSQL credentials ---
# These are used for local PostgreSQL setup
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=my_database

# --- JWT Secret ---
# Used for signing JSON Web Tokens in your application
JWT_SECRET=your_jwt_secret_here

# --- Test database URL ---
# Used during testing to avoid interfering with the main database
# Note the schema parameter: ?schema=public
TEST_DATABASE_URL="postgresql://test_user:test_password@127.0.0.1:5433/test_database?schema=public"
